// ERROR
// present the error message? its not realisitc otherwise?

// this example can be huge...
// On what 3 lines are there spelling mistakes?
//alternatives? then can guess
// How do i implement this in the solution?
// ask for lines?

// Find 2 spelling errors
 function add()
 {
     var fruits = ["banana", "apple", "pineapple", "kiwi"];
     var count = 0;

     for (var i = 0; i < fruits.lengt; i++) {
         var fruit = fruits[i];

         if( fruit = apple" ) {
             count += 2;
         }
         else {
             count++;
         }
     }
     return count;
 }


// not capitalized letter
// misspell a word that will change color 


// finding errors with color? shouldnt work?

// is it becasue they have training with it? or becasue it actually better? they dont have with semantic though...

// Find 5 errors.
// is this really a plausible task?
